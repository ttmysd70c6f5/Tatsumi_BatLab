The workspaces in this directory will be used for unit testing during the ‘playback’ test.  To run all tests, from the command line run:

Trodes test 

To run just the playback test, run:

Trodes test playback